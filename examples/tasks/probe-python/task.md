Determine whether a Python 3 interpreter is available in this environment.

Write the file `/artifacts/answer.md`. Its entire content must be `PYTHON` if a Python 3 interpreter can be executed, or `NO-PYTHON` if it cannot, followed by a newline. Do not write anything else.
