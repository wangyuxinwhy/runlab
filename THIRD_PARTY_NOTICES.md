# Third-Party Notices

RunLab macOS release archives include an unmodified static `runc` binary from the [Open Container Initiative runtime project](https://github.com/opencontainers/runc), version 1.5.1.

`runc` is licensed under the Apache License 2.0. Each archive containing `runc` also carries the upstream `LICENSE` and `NOTICE` files as `RUNC-LICENSE` and `RUNC-NOTICE`. The upstream binary, license, and notice are downloaded from the `v1.5.1` release or tag and verified against digests fixed by RunLab's release workflow.
