name: Publish Cargo packages

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing vMAJOR.MINOR.PATCH release tag
        required: true
        type: string
      authentication:
        description: Use a bootstrap token only for first publication, then Trusted Publishing
        required: true
        default: trusted-publisher
        type: choice
        options:
          - trusted-publisher
          - bootstrap-token
      operation:
        description: Publish the tag or only verify the Trusted Publisher token exchange
        required: true
        default: publish
        type: choice
        options:
          - publish
          - verify-auth

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    environment: release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ inputs.tag }}
      - name: Validate tag and version
        run: |
          tag="${{ inputs.tag }}"
          [[ "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          git rev-parse --verify "refs/tags/$tag^{commit}" >/dev/null
          version="${tag#v}"
          manifest_version="$(sed -n 's/^version = "\([^"]*\)"/\1/p' Cargo.toml | head -n 1)"
          test "$version" = "$manifest_version"
          grep -F "## [$version]" CHANGELOG.md >/dev/null
      - name: Verify normalized Cargo packages
        run: scripts/verify-packages.sh
      - name: Authenticate with crates.io Trusted Publishing
        if: inputs.authentication == 'trusted-publisher'
        id: trusted-publisher
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18
      - name: Publish in dependency order
        if: inputs.operation == 'publish'
        env:
          CARGO_REGISTRY_TOKEN: ${{ inputs.authentication == 'trusted-publisher' && steps.trusted-publisher.outputs.token || secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          test -n "$CARGO_REGISTRY_TOKEN" || {
            echo "the selected crates.io authentication mode produced no token" >&2
            exit 1
          }
          scripts/publish-crates.sh
      - name: Verify Trusted Publisher authentication
        if: inputs.operation == 'verify-auth'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.trusted-publisher.outputs.token }}
        run: |
          test "${{ inputs.authentication }}" = trusted-publisher || {
            echo "verify-auth requires trusted-publisher authentication" >&2
            exit 1
          }
          test -n "$CARGO_REGISTRY_TOKEN" || {
            echo "crates.io returned no Trusted Publishing token" >&2
            exit 1
          }
          echo "crates.io Trusted Publisher token exchange succeeded"
